<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shop.tripn.oracle.mappers.PublisherMapper">
	<select id="findAll" resultType="shop.tripn.oracle.mappers.PublisherMapper">
	select
	    pub_id pubId,
	    pub_name pubName,
	    mgr_name mgrName,
	    phone
    from publishers
	</select>
	<select id="findById" resultType="shop.tripn.oracle.mappers.PublisherMapper"
	parameterType="int">
	select
	    pub_id pubId,
	    pub_name pubName,
	    mgr_name mgrName,
	    phone
    from publishers
    where pub_id like ${pubId}
	</select>
</mapper>